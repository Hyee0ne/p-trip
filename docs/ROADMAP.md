# ROADMAP.md — P의 여행 개발 로드맵 v2

Claude Code 세션 단위로 쪼갠 태스크. 위에서 아래로 진행하고, 완료 시 `[x]` 커밋.
각 마일스톤은 "데모 가능한 상태"로 끝나야 한다.

> **v2 변경 (2026-08-26)**: 거점 역진입 · DR-05 동승자 · DR-06 백그라운드 알림 ·
> 급상승 실데이터가 MVP로 승격. 프로토타입 HTML을 직접 생성하기로 결정(M0.3 신설).
> 총 11~15일 → **약 19~20일**.

---

## 병렬 트랙 — 오늘 바로 신청 (코드로 우회 불가, 리드타임 있음)

M1이 전부 여기 걸려 있다. M0 코딩과 **동시에** 진행할 것.

---

## 🚀 M6. 출시 (2026-08-30 결정 — 출시가 우선, 공모전은 그 결과물로 낸다)

- [ ] **data.go.kr 운영계정 신청** ← 승인 대기가 제일 길다. 가장 먼저
      국문 관광정보 서비스_GW(B551011) · 관광지 연관 관광지(B551011) ·
      출몰시각(B090041) · 천문현상(B090041). 전통시장은 CSV라 신청 불필요
- [ ] **파이프라인 전국화** (승인 전에 미리 짠다)
      · 시군구 3개 하드코딩 → 전국 순회 (재개 가능하게)
      · 손으로 찍은 회랑 좌표 → **`routes.geom` 51선으로 PostGIS 판정**
      · 코스 하드코딩 목록 → 노선을 구간으로 잘라 자동 생성
- [ ] **홈 위치 기반** — 지금 `spots()`·`courses()`에 좌표 필터가 없다.
      부산에서 켜도 강릉 코스가 나온다. 데이터가 전국이 되면 바로 결함이 된다
- [ ] 데이터 없는 지역의 정직한 빈 상태
- [x] **데모 모드를 개발 빌드에만** (2026-08-30) — `Env.demoAvailable`
      ⚠ 실기기 테스트 빌드엔 `--dart-define=DEMO_BUILD=true`를 넣어야 한다
- [x] ~~로그인/백업~~ → **붙이지 않는다** (2026-08-30 재확인). 기기 유실은 감수한다
- [ ] 스토어: 개인정보처리방침 · 아이콘 · 스크린샷 · 지원 URL · 연령등급 · TestFlight
- [ ] **백그라운드 위치 심사 문구** — App Store에서 가장 까다로운 항목

---

## 🔻 남은 작업 (2026-08-30 기준)

**막고 있는 것 (결정 필요)**
1. **묵호등대** — TourAPI에 없다. 대본을 「동해 논골담길(등대 담화마을)」로 바꿀지 결정 (M1 §검수)
2. **시연 날짜** — 정해지지 않았다. 장날(끝자리 2·3·7·8)이 아니면 ×3 가중치도
   DR-06 발신 조건도 한 번도 안 걸린다

**실기기 확인 완료 (2026-08-30)**
- CO-06 지도 핀 찍기 · 장소검색 · 포그라운드 TTS 낭독

**실기기에서만 확인되는 것** — 코드는 완료, 눈으로 볼 일만 남았다
3. 카카오내비 **경유지**(viaList) 실호출 · TTS 덕킹(내비 안 끊기는지)
4. 사진 스트립 (photo_manager 권한 + 실제 사진 매칭)
5. DR-06 백그라운드 음성·알림 — **실제 주행이 필요하다**
6. 시연 리허설 전 구간

**만들 수 없는 것 (막힌 이유가 분명함)**
7. 공유 카드 **딥링크** — 여행기가 기기 안에만 있고(원칙 5) 배포 URL도 없다.
   서버 저장을 열거나 배포 주소가 생기기 전까지는 이미지만 공유한다
8. 데이터랩 급상승 — 키 미발급. 연관관광지 2시점으로 대체됨

**끝난 것 (2026-08-30)**
- CO-06 지도 핀 찍기 — **가짜 지도(그라데이션 상자)를 진짜 카카오맵으로 교체**
- CO-06 오늘 밤 하늘 한 줄 · CO-02 거점 배너 '설정됨'
- CO-06 **카카오 장소검색** — Edge Function `search_places`. 검색창이 실제로 동작한다
- **SCREENS 17개 화면이 전부 구현됐다.** 남은 건 실기기 확인과 데이터 품질뿐이다

**품질 (여유 있으면)**
9. 사진 없는 스팟 71% — 전면 카드가 색면으로 뜬다. TourAPI 사진 재수집 필요
10. 7번 국도 실주행 1회 — 발견 타이밍 체감 조정

---

- [x] data.go.kr: TourAPI — 국문관광정보 · 관광사진 · 연관관광지 (2026-08-29)
      ⚠ **고캠핑·두루누비는 안 붙였다.** 데모 구간에 필요 없었고 호출만 늘린다
- [x] data.go.kr: 전국전통시장표준데이터 (2026-08-29) — markets 11건
- [x] data.go.kr: 한국천문연구원 출몰시각 + 천문현상 (2026-08-29) — sun_moon 585 · astro_events 198
- [ ] ~~한국관광 데이터랩~~ → **연관관광지 2시점으로 대체** (2026-08-29). 키 미발급
      `spot_links` 202603↔202606 비교로 '순위 오른 연결' 19건. 데이터랩 없이 급상승이 나온다
      ⚠ `fetch-datalab.ts`는 남아 있으나 **돌지 않는다**. 키가 생기면 그때 켠다
- [x] developers.kakao.com: 네이티브 앱 키 — 지도·내비 (2026-08-27)
      ⚠ **로그인은 활성화하지 않는다** (원칙 5 — 로그인을 넣지 않는다)
- [x] developers.kakaomobility.com: 길찾기 REST 키 (2026-08-30)
      `supabase secrets`에만 있다. 앱 번들에 없다 — Edge Function `compare_routes` 전용
- [x] Supabase 프로젝트 생성 (2026-08-29) — `p-trip` / ref `xbuyxpikbqkwovfnbrzz`

---

## M-1. 스펙 정합화 (반나절) — 코드 이전에

문서 4개 사이의 충돌을 먼저 닫는다. 안 닫으면 M2~M3에서 매번 멈춘다.

- [x] SCREENS.md: **CO-03 스팟 상세** 절 추가 (마을/스팟 통합 확정)
- [x] SCREENS.md: **CO-06b 국도 제안** 절 추가 + CO-06에 `/base` 역진입 경로 반영
- [x] SCREENS.md: **DR-05 동승자 모드 / DR-06 백그라운드 알림** 절 추가
- [x] TECH_SPEC §2: trust_score 배점표, detour_min 정의, exit_geom/exit_frac,
      markets 끝자리 정규화, trips status·nullable, saves CHECK 제약
- [x] TECH_SPEC §3.1: 진출로 역산 타이밍 + 정차 시 heading 필터 해제
- [x] TECH_SPEC §3.2: 장날 판정식 수정 (10·20·30 → 0 정규화, 월말 오판 검수)
- [x] TECH_SPEC §3.3 / §3.7: 길찾기 REST 예외 명문화 + 티맵 보조, §3.4 권한 2단계
- [x] TECH_SPEC §4·§5·§6: 데이터랩·길찾기 키 추가, 라우트 표 갱신, 스코프 재정리
- [x] CLAUDE.md: 원칙 1 예외 조항, 원칙 3 외부 링크 허용 명시, strings.dart 규약
- [x] **신규 절 4개 확정** — CO-03 · CO-06b · DR-05 · DR-06 모두 구현·검증까지 끝났다
      (DR-06은 2026-08-29 iOS 기준으로, CO-06b는 2026-08-30 실데이터로 다시 확정)

## M0. 프로젝트 셋업 (반나절)

- [x] git init + 모노레포: `app/`(Flutter) · `pipeline/`(Node+TS) · `supabase/`
- [x] **CLAUDE.md를 리포 루트로 이동**
- [x] Flutter 버전 결정 → **핀 고정** (go_router 17.5.0 / flutter_riverpod 3.3.2). Flutter 업그레이드는 보류
- [x] Riverpod + go_router 3탭 셸 + 라우트 테이블 전체 선언 + 뷰 모드 글로벌 상태
- [x] `.env.example` + `dart_defines.example.json` + `core/env.dart`, `.gitignore` 정비
- [x] Supabase CLI 설치 + `supabase init` + **brrrp 격리**(project_id·포트·파이프라인 가드)
- [x] Supabase 프로젝트 연결(`p-trip`, ap-northeast-1) + PostGIS 활성화 — 2026-08-29

## M0.3. 디자인 시안 (1~1.5일) ★신규 — 프로토타입 HTML을 직접 만든다

산출물: `docs/P의여행_프로토타입_v2.html` (SCREENS.md·CLAUDE.md가 이 파일명을 참조).
Figma는 만들지 않는다 — CSS 변수 → `theme.dart` 1:1 이식이 목적.

- [x] mobbin MCP로 레퍼런스 수집 (다크 레이더 화면, 바텀 오버레이 카드, 지도 상세)
- [x] ui-ux-pro-max 스킬로 팔레트·타이포·간격·모션 확정
      (CLAUDE.md 색 6개를 **고정 시드**로, 나머지 토큰을 파생)
- [x] 프로토타입 HTML — **시연 플로우 7화면 우선**:
      CO-01 / CO-07 / CO-02 / CO-06 / DR-01 / DR-02 / MY-02
- [x] 공통 컴포넌트 6종: RouteBadge(sm/md/lg) · 탭바 · 토스트 · 스팟카드 · 확신도 문구 · 하트
- [x] `core/theme.dart` 이식 (RouteBadge 위젯은 M2에서 화면과 함께)
- [x] Pretendard 폰트 5종(400~800) + OFL 라이선스 번들
- [x] `core/strings.dart` — SCREENS.md 고정 카피 전량 상수화

## M0.5. 관통 스파이크 (반나절) — 버릴 코드 전제

여기서 막히면 M1 시작 전에 설계를 고친다.

- [x] **지도 플러그인 확정 — `kakao_map_sdk` 1.2.6 (네이티브 SDK v2 래퍼)**. 2026-08-29.
      WebView 기반 `kakao_map_plugin`에서 갈아탔다. pod install·Xcode 빌드 통과 확인.
      네이티브 렌더링이라 폴리라인 개수 부하 문제가 사라진다 — 가시영역 필터도 불필요.
      ⚠ 실제 타일 렌더는 **네이티브 앱 키**가 있어야 확인된다
- [x] **지도 실렌더 확인 (2026-08-29)** — 네이티브 앱 키 주입 후 동해시 기준 타일·라벨·
      현재 위치 마커·줌 컨트롤 정상. 시뮬레이터 + 실기기(iPhone 12 mini) 둘 다.
      KakaoMapsSDK.framework arm64 단일 — 슬라이스 오염 없음
- [x] 노선 MultiLineString 실렌더 (2026-08-29) — 51선 중 **선형 있는 것 50개**
      ⚠ LineString으론 부족했다. 77번 국도가 675km인데 396점으로 잘렸다 — 체인 63개 중 61개를 버리고 있었다
- [ ] `NaviApi.navigate(viaList:)` **실기기** 1회 호출 — 코드는 완료(2026-08-30), 눈으로 확인만 남음
- [x] geolocator 스트림 (2026-08-29) — `DriveNotifier.startLive`
- [ ] heading/speed **실측**으로 §3.1 방향 필터 임계값 조정 — 실주행 필요

## M1. 데이터 파이프라인 (2.5일) ★데모 품질의 8할

- [x] **데이터 실측 스파이크 (0.5일)** — 2026-08-29 실행. 결과:
      · TourAPI **KorService2** 확정. 동해 반경 5km **26건**. 목록엔 개요·영업시간이 없어
        detailCommon2/detailIntro2를 따로 불러야 한다
      · **trust_score 게이트 60 통과율 88%(8건 중 7건).** 배점표 그대로 간다.
        ⚠ 다만 **전화번호가 8건 중 1건뿐** — CO-03 "전화 원탭"이 데모 구간에서 거의 안 뜬다
      · 출몰시각에 **월출·월몰·박명(시민/항해/천문) 전부 포함** → §3.8 별 보기 좋은 밤 계산 가능 확정
      · 천문현상 2026년 198건. **186건은 astroTitle이 비어 있고 astroEvent만 있다**(삭·망·근지점 등
        월상 달력). 이름 붙은 '사건'은 12건 — 유성우 3, 개기월식 1, 계절 별자리 4, 보름달·행성 등.
        **적재 때 둘로 가른다**: 노출용 이벤트 / 월령 계산용 달력
      · ✅ 연관 관광지 — `baseYm` 수용 확인. rlteTatsNm/rlteRank/3단 분류가 온다.
        **두 달을 비교하면 데이터랩 없이도 급상승 산출 가능** (단 rlteRank 절대순위는 쓰지 않는다)
      · ✅ 전통시장 — **오픈API가 없다.** 연 1회 갱신 CSV 파일로 간다(pipeline/data/markets.csv).
        1,393곳 · 개설주기 표기 8종 · **북평민속시장 3일+8일 확인**(동해시 오일장길 32)
        데모 구간 5일장은 북평(3·8)과 삼척중앙(2·7) 둘뿐
- [x] ~~착수 전 필수 확인~~ (위에서 완료):
      · 데이터랩이 **2개 시점**을 주는가 (안 주면 "조용히 뜨는 길" 변화율 불가)
      · 연관관광지 API가 기준연월 파라미터를 받는가 (안 받으면 "차량 유입 급증" 불가)
      · 데이터랩 입도(시군구) ↔ 코스/스팟 매칭 방법 확정
      · TourAPI 쿼터·실제 응답 스키마 (문서와 다른 필드 주의)
      → 결과에 따라 아래 두 태스크의 구현 방식이 갈린다. **결과 먼저 공유**
- [x] 마이그레이션 5개 적용 (2026-08-29) — routes/courses/spots/spot_links/markets/events/
      astro_events/sun_moon/profiles/saves/trips/trip_points/trip_stops/trip_photos + RLS + GRANT
      ⚠ **GRANT를 따로 줘야 한다.** 마이그레이션이 postgres 역할로 안 돌아서 Supabase 기본
        default privileges가 안 걸린다 — 빼먹으면 anon도 service_role도 42501이 난다
      검증: 공개 읽기 200 / 익명 쓰기 401 / 사용자 테이블 익명 401 / service_role 쓰기 201 /
            is_market_day([3,8], 9월3일)=true, 9월5일=false
- [x] `build-routes.ts` (2026-08-29) — 51선 메타 + **전국 노선 선형**
      국토교통부 일반국도 도로중심선(15122482) SHP → 51선 중 **50개 적재**, 점 181,385개
      총 연장 **13,910km** — 기획 표기 14,000km와 일치. 7번 국도 508km
      ⚠ 50번은 원본에 없다 (섬 구간 미개통으로 추정)
      ⚠ 국도는 도심통과·우회로 실제로 끊겨 있다 → **MultiLineString**으로 담는다.
        한 갈래로 억지로 이으면 77번이 675km→396점이 된다 (63조각 중 2개만 남음)
      ⚠ .dbf는 EUC-KR이다. .cpg를 읽어 인코딩을 준다 — 안 주면 '노선번호'를 못 찾는다
      ⚠ '일련번호'가 '노선번호'보다 먼저 매칭되지 않게 정확한 필드명을 우선한다
- [x] `fetch-tourapi.ts` (2026-08-29) — **지역기반 + 법정동 코드 + 회랑 10km** 필터
      ⚠ 위치기반(locationBased)이 아니다. `areacode`가 비어 오는 레코드가 있어
        `lDongRegnCd`/`lDongSignguCd`로 갈아탔다 (동해시 48건 → 116건)
      ⚠ 동시 8개로 돌리면 **초당 요청 수 초과**로 거부당한다. 4개 + 250ms로 낮췄다
      → type 매핑, **trust_score 배점표 적용**, exit_geom/exit_frac/detour_min 계산
- [x] 확장 병합: 관광사진 · 행사(events 4건) (2026-08-29)
      ⚠ 고캠핑·두루누비는 **안 붙였다** — 데모 구간에 필요 없고 호출만 늘린다
- [x] 연관 관광지 → spot_links (2026-08-29) — 211건, 순위 오른 연결 19건
      `baseYm`으로 두 시점(202606/202603)을 각각 받아 비교 → **데이터랩 없이 급상승 산출 가능**
      예: 묵호항→망상오토캠핑리조트 38위→13위, 추암해변→어달해변 25위→16위
      ⚠ 좌표를 안 줘서 이름으로만 붙는다. 5,099건 중 109건만 붙은 건 스팟이 625건이라서다
- [x] **진출점 계산** (2026-08-29) — `compute_spot_exits()` RPC. route_id·exit_geom·detour_min
      7번 국도에 596건. 국도 10분 이내 453건, 그중 **게이트 통과 133건 = 레이더 후보**
      ⚠ routes.geom에 GIST 인덱스가 없어 statement timeout이 났다. 인덱스 + 배치로 해결
      ⚠ exit_frac은 아직 없다 — geom이 MultiLineString이라 ST_LineLocatePoint를 못 쓴다.
        코스(courses.geom)가 생기면 그걸 기준으로 낸다
- [x] `fetch-markets.ts` (2026-08-29) — 회랑 12km 안 11곳. **북평민속오일장 3·8일 · 85점 · 사진○**
      삼척 중앙시장 2·7일도 85점 — 시연일이 3·8일이 아닐 때의 대안
      ⚠ **시장을 새 스팟으로 만들지 않고 기존 TourAPI 스팟에 붙인다.** CSV엔 사진·개요가 없어
        새로 만들면 25점짜리가 되고 게이트(60)를 못 넘어 레이더 카드로 못 뜬다
      ⚠ 이름 매칭을 '앞 두 글자'로 했다가 **음식점·축제를 시장으로 바꿔놨다**
        (삼척번개시장→삼척동해왕이사부축제, 강릉서부시장→강릉 오금집).
        지명이 앞 두 글자라 아무거나 걸렸다 → 상대 이름에도 '시장'류 단어를 요구하게 고침
- [x] `fetch-sunset.ts` (2026-08-29) — 45일 × 13격자 = **585건 캐시**
      일몰만이 아니라 월출·월몰·박명 3종을 같이 받는다 (§3.8 전제)
      격자는 **스팟이 있는 칸만** 만든다 — 빈 바다·산을 채울 이유가 없다
      ⚠ 실데이터로 `moonless_after_dusk`를 검증하니 **틀렸다.** 응답의 moonset은 그날 아침에
        진 달이라 moonrise와 짝이 아닌데, 'moonset < moonrise'를 '이미 졌다'로 읽고 있었다.
        8/29(월출 19:23, 박명 20:30)이 '별 보기 좋음'으로 나왔다 — 밤새 달이 떠 있는 날인데.
        고친 뒤: 8/29~31 달빛 있음, 9/1부터 좋음으로 정상
- [ ] ~~`fetch-datalab.ts`~~ → 연관관광지 2시점으로 대체. 키가 생기면 켠다
- [x] **코스 생성** (2026-08-29) — `build-courses.ts`
      「동해 바닷길」 7번 국도 삼척–강릉 · **65km · 순수 주행 87분 · 점 883개**
      노선 선형을 bbox로 자르고 가장 긴 갈래를 코스 LineString으로 삼는다
      ⚠ **노선을 기계적으로 N등분하지 않는다.** 그건 큐레이션이 아니라 토막이다.
        데모 하나를 제대로 만들고 나머지는 사람이 정할 때 늘린다
      ⚠ 픽스처가 86km/130분이었는데 실제 선형은 65km/87분이다 — 지어낸 값이었다
      exit_frac 611건 채움 → 진출로 역산(§3.1) 가능.
      검증: 삼척 0% → 추암 16% → 북평 20% → 어달 37% → 정동진 63% → 강릉 100%
- [x] 검수 스크립트 `npm run verify` (2026-08-29) — 11개 항목.
      **데모를 실제로 깨뜨릴 것만** 본다. 치명적인 게 걸리면 종료코드 1.
      항목: 51선 선형 / 레이더 후보 수 / 진출점 미할당 / 결측률 / **데모 대본 스팟 존재** /
            장날 · **월말 오판(31일)** / 해·달 캐시 / 천문현상 / 발자국 두 시점 / 코스
      2026-08-30 재검수: 통과 9 · 주의 1(사진 없음 71%) · **실패 1 — 묵호등대**
      ⚠ 이어받기를 시도했으나 **TourAPI가 HTTP 429**다. 초당 제한인 줄 알고 4개+250ms로
        낮췄는데 단발 호출도 429 — 계정 한도라 오늘은 더 못 받는다
      ⚠ **묵호등대는 TourAPI 목록에 아예 없을 가능성이 높다.** 대신 같은 언덕의
        **「동해 논골담길(등대 담화마을)」(신뢰도 65)** 이 들어와 있다.
        데모 대본 4곳 중 3곳(북평민속오일장 85 · 추암 촛대바위 75 · 어달해변 40)은 있다
        → **결정 필요**: 대본을 논골담길로 바꿀지, 할당량 회복 후 재시도할지
      ⚠ 어달해변은 신뢰도 40이라 **게이트(60)에 걸려 레이더 카드로는 안 뜬다**
- [x] **CO-07 지도 전환** (2026-08-29) — 지도 전면 + 드래그 바텀시트 3단,
      현재 위치 마커, 가시영역 노선 필터, 권한 거부 폴백(남한 전체 + 시트 펼침)

## M2. 발견 탭 (3.5일)

- [x] **CO-06 지도 핀 찍기** (2026-08-30) — `base_pin_map.dart`
      ⚠ **처음엔 거점 화면의 스크롤 목록 안에 작은 지도를 박았다가 실기기에서 자리가
        통째로 비어 나왔다.** 카카오 지도는 네이티브 플랫폼 뷰라 스크롤 안에서 합성되지 않는다.
        CO-07처럼 **화면을 채우는 별도 화면**으로 옮겨서 고쳤다
      ⚠ 위젯 테스트로는 못 잡는다 — 테스트엔 지도 키가 없어 대체 위젯이 그려진다.
        **실기기에서만 드러나는 종류다**
      핀을 POI로 그리지 않는다. 가운데 십자를 Flutter로 얹고 지도를 움직여 맞춘다 —
      마커 API를 안 쓰는 만큼 실패할 곳이 줄고 손가락에 가리지도 않는다
      **실기기 확인 완료** (2026-08-30)
      ⚠ 그전엔 **가짜 지도**였다. 그라데이션 상자에 핀 아이콘을 올려둔 장식이라
        안내문("핀 하나만 찍으면 레이더의 기준점이 됩니다")이 거짓말이었다
      `onMapClick`으로 찍은 자리가 거점이 된다. 후보 목록에서 고르면 지도도 따라간다
      이름 없는 자리는 '지도에 찍은 위치'로 적는다 — 지어내지 않는다
      키가 없거나 지도가 실패하면 **가짜 지도를 그리지 않고** 이유를 적는다
- [x] **CO-06 오늘 밤 하늘** (2026-08-30) — 핀을 찍은 뒤에만, 조건 맞을 때만
      ⚠ SCREENS 예시는 "달이 01:20에 집니다"인데 **그 값을 낼 수 없다** —
        moonset이 그날 아침에 진 달이라 짝이 안 맞는다. 낼 수 있는 사실만 말한다
- [x] **CO-02 거점 배너 '설정됨'** (2026-08-30)
      ⚠ 상태를 아예 안 읽어서, 거점을 정하고 돌아와도 계속 '정해주세요'로 보였다
- [x] **CO-06 카카오 장소검색** (2026-08-30) — Edge Function `search_places`
      ⚠ 그전엔 검색창이 `TextField`가 아니라 **`Text`**였다. 탭해도 아무 일도 안 났다 —
        후보 목록에 없는 숙소를 잡은 사람은 거점을 정할 방법이 없었다
      타이핑이 멈춘 뒤 400ms에 한 번 부른다. 글자마다 부르면 할당량만 태운다
      ⚠ **범위를 막지 않는다.** 현재 위치를 주면 가까운 순 정렬만 —
        "어디서 예약했든 상관없어요"가 이 화면의 안내문이다
      ⚠ 결과는 **이름·주소·좌표만** 받는다. 전화·카테고리·예약 링크를 안 받는 게 방어다 (원칙 4)
      ⚠ 카카오 REST 키는 **길찾기와 같은 키**다. `.env`에 두 이름으로 있던 걸 하나로 모았다 —
        같은 값을 두 곳에 두면 나중에 하나만 갈고 헤맨다

- [x] ON 온보딩 3장 — 권한은 요청만, 건너뛰기 허용
- [x] CO-01 홈: 뷰 모드 2개(한 곳씩/훑어보기), 국도 레일, 큐레이션 3축. 급상승은 실데이터 대기 중이라 섹션 숨김
- [x] CO-07 국도 선택: **지도 전면 + 끌어올리는 바텀시트 3단**(SCREENS.md CO-07, 2026-08-29).
      내 주변/그 밖의 길 두 섹션, 남북·동서 필터, 위치 상태 4분기(측정 중·거부·영구거부·서비스꺼짐).
      위치는 geolocator 1회 측정 — 실기기 경로까지 확인함.
      ⚠ 지도 타일·노선 선형은 카카오 키와 M1 GeoJSON 대기. 그전까지 지도 자리는 비워 둔다
- [x] SR 검색: 별도 모달 화면(흰 배경·리스트·토글 없음), 최근 검색, 필터 한 줄
- [x] CO-02 코스 상세: 경로 미리보기(SDK 미사용), 거점 배너, 뷰 모드별 발견 표시, 출발 CTA
- [x] CO-03 스팟 상세: 사진/영업/확신도 문구/전화 원탭/spot_links/네이버 후기 링크
- [x] CO-06 거점 설정: 후보 리스트 + 확정 → 거점 글로벌 상태. ⚠ 카카오 장소검색·지도 핀은 키/M0.5 대기
- [x] `/base` 역진입 라우트 + CO-06b 모달 UI. ⚠ compare_routes Edge Fn은 키 대기 —
      소요시간을 지어내지 않으므로 그때까지 시트가 안 뜨는 게 정상 동작
- [x] HND 핸드오프 — **실호출까지 완료** (2026-08-29)
      `NaviApi.navigate`에 좌표·경유지·무료도로 우선(RpOption.free)을 넘긴다
      ⚠ 세 군데가 빠져 있어 버튼이 조용히 아무것도 안 했다:
        · **좌표를 안 넘겼다** — 딥링크에 이름만 있었다. Spot에 lat/lng를 추가
        · **iOS `LSApplicationQueriesSchemes`가 없었다** — 선언 안 하면 canLaunchUrl이 항상 false
        · **`KakaoSdk.init`을 안 불렀다** — NaviApi가 조용히 실패한다
      ⚠ 미설치 폴백이 `market://`(안드로이드 전용)뿐이라 iOS에선 아무것도 안 열렸다.
        플랫폼별로 App Store / Play로 나눴다
- [x] saves(like) 저장/해제 — 모든 하트가 한 상태를 공유. 마이 탭 목록은 M4

> ✅ 2026-08-29: **실데이터로 전환 완료.** `SupabaseDiscoverRepository`가 붙었고
>   키가 있으면 실데이터, 없으면 픽스처로 떨어진다(위젯 테스트가 키 없이 돌아야 한다).
>   확인: 전면 카드에 TourAPI 실사진, CO-07 지도에 7번 국도 선형.
>
> ⚠ (지난 기록) 2026-08-27: API 키 대기 중이라 **픽스처 데이터로 화면부터** 구현 중.
>   `FixtureDiscoverRepository`가 임시값을 주고, M1 이후 `providers.dart` 한 줄만 교체한다.
>   **시연·제출은 반드시 파이프라인 실데이터로 한다.**

## M3. 레이더 (6일) ★킬러 데모

- [x] **데모 모드(7번 국도 mock 주행)** (2026-08-30) — `core/drive.dart`
      **실제 코스 선형(courses.geom 883점)** 위를 걸어간다. 좌표를 지어내지 않는다.
      진행률·누적거리·진행방향·속도를 내고, 레이더가 그걸 본다.
      배속 기본 20 (65km ≈ 3분). `--dart-define=DRIVE_SCALE=10`으로 조절
      ⚠ 실주행 전환은 `DriveNotifier`만 geolocator 스트림으로 갈아끼우면 된다
- [x] **진출로 역산** — 카드가 시간이 아니라 **위치**로 뜬다.
      `minutesTo(exitFrac)`가 3~7분 구간일 때만 노출 (§3.1 5번)
      ⚠ 레이더 블립을 8개로 제한했다. 30개를 다 찍으니 라벨이 서로 덮여 못 읽는다 —
        운전 중 화면이고, 레이더는 목록이 아니라 '살피는 중'이라는 시각화다
- [x] **DR-00 위치 권한 + geolocator 스트림** (2026-08-29) — 거절하면 이유 + [설정] [데모 모드]
- [x] DR-01 레이더 UI: 동심원 3링 + 스윕 애니메이션 + 유형색 블립 + 거점/기록 칩
- [x] **`discover_ahead` RPC** (2026-08-29) — PostGIS 반경+방향, 게이트는 호출부에서 가른다
      (DR-01은 게이트 적용, DR-05 동승자는 미적용 — §4-5)
- [x] DR-02 근접 카드: 전면 카드 + 원형 액션 3종(82pt/60pt) + 검증 배지. ✕는 passed 적립
- [x] **타이밍 가중치 + 중복 억제** (2026-08-30) — §3.1 4·6번
      장날인 시장 ×3 / 일몰 −60~−20분 뷰포인트 ×2 / 11–14시 음식점 ×2 / 사진 있으면 +0.5
      같은 유형 연속 금지 · **주행 30분당 2회** (시연 배속이 아니라 여정 기준)
      ⚠ 점수를 화면에 내보내지 않는다 — 순서를 정하는 데만 쓴다 (원칙 3)
      ⚠ **장날 카드는 시연일이 장날이어야 뜬다.** 북평 3·8일 / 삼척중앙 2·7일 —
        한 달에 2·3·7·8·12·13·17·18·22·23·27·28일 열두 날. **시연일을 여기 맞출 것**
- [x] **DR-03 몰아보기** (2026-08-30) — 정차 3분 + 스쳐간 것 2개 이상이면 시트가 뜬다
      가로 카드 · [지금 가기](→HND) [계속 찜] [지우기]
      ⚠ **"되돌아가기" 유도 문구를 쓰지 않는다.** 갈지 말지는 사용자가 정한다 —
        테스트가 그 문구가 없는지 확인한다
      ⚠ 정차 판정은 **주행 시간** 기준이다. 시연 배속과 무관하게 '3분 멈춤'이어야 한다
      들르기를 누르면 주행이 멈추고(도착), 시트를 닫으면 다시 달린다
- [x] **핸드오프 viaList — 들르기가 거점을 지킨다** (2026-08-29)
      ⚠ 그전엔 「들르기」가 **목적지를 발견으로 바꿔서 오늘 밤 잘 곳이 사라졌다.**
        달리는 중 + 거점 있음 → 거점이 목적지, 발견이 경유지 (카카오내비 최대 3곳)
      ⚠ 이건 경로 편집이 아니다. 돌고 있는 세션에 경유지를 꽂는 API는 없다 —
        내비가 새로 열려 현재 위치에서 다시 계산한다. 다만 거점을 잃지 않는다
      ⚠ 둘러보다 누른 경우(주행 중 아님)엔 지킬 목적지가 없다 → 그 곳으로 바로 간다
      시트가 목적지·경유를 나란히 보여준다 — 안 보여주면 목적지가 바뀐 줄 알고 불안해진다
      티맵은 경유지를 못 받는다(버튼 문구가 '목적지만'인 이유). 미설치 분기는 이미 있음
      ⚠ **실기기 확인 남음** — 에뮬레이터로는 내비가 안 열린다
- [x] **여행 기록** (2026-08-30) — `core/trip_log.dart`. 주행이 여행기가 된다.
      출발 시 여행 생성 → 0.5km마다 경로 기록 → 들르기/스쳐감을 stop으로 → 마치기
      ⚠ **기기 안에 둔다** (로그인 없음, 원칙 5). 서버 trips·RLS는 남겨뒀다
      ⚠ '오늘 여행 마치기'가 하드코딩된 `/my/trip/ep3`로 가고 있었다 — 방금 끝낸 여행으로 간다
      ⚠ 픽스처 여행기를 지웠다. 한 번도 안 달렸는데 기록이 있으면 안 된다
- [x] **DR-05 동승자 모드** (2026-08-30) — `discover_ahead` RPC + 카드 덱 스와이프
      **경로가 아니라 현재 위치+방향 기준**이다 (원칙 2). 반경 20km, ±60°, **게이트 미적용**
      검증: 동해에서 전방향 15건 / 북 8 / 남 5 / 동 3(바다) — 방향 필터가 실제로 거른다
      좌 스와이프 = 넘기기 (**passed 적립 안 함** — 훑어본 것과 지나친 것은 다른 사건)
      우 스와이프 = 찜 + 다음 정차지 후보 · 탭 = CO-03
      빈 상태 "이 앞은 잠시 조용해요" + [더 멀리 보기](반경만 넓힌다)
      ⚠ family 키에 주행 좌표를 그대로 쓰면 250ms마다 새 provider가 생겨 영원히 로딩이다.
        2km 움직이거나 30° 돌았을 때만 다시 묻는다
- [x] **DR-02 음성 낭독 + 15초 무응답** (2026-08-29) — `core/voice.dart`, ko-KR 0.48배속
      15초는 **실시간**이다. 쿨다운·정차와 달리 여정이 아니라 사람의 반응 시간이라 배속과 무관
      ⚠ SCREENS 344행("찜에 담아둘게요")과 370행(passed)이 서로 달랐다. **동작이 passed**이고
        ✕도 passed라, 문구를 "스쳐간 발견에 담아둘게요"로 맞췄다 — 화면이 거짓말하지 않게
      **실기기 확인 완료** (2026-08-29): 포그라운드 낭독 정상
      ⚠ 처음엔 실기기에서 **소리가 안 났다.** flutter_tts는 `setIosAudioCategory`로 카테고리만
        잡고 `setActive`를 안 부른다 — 활성화는 `setSharedInstance`라는 별도 메서드다.
        시뮬레이터는 그래도 났다(오디오 큐 22050Hz 생성을 로그로 확인). 이 차이가 원인이었다.
        `speak()`마다 `setSharedInstance(true)`로 우리 카테고리를 켠다
      ⚠ 덕킹(카카오내비 안 끊기)은 넣었으나 **주행 중 동시 실행 확인은 남았다**
- [x] **실주행 전환 + DR-00 위치 권한** (2026-08-29) — `DriveNotifier.startLive`
      같은 코스 선형 위를 진짜 GPS로 달린다. **마이 탭 「데모 모드」 토글이 진짜로 갈아탄다**
      (그전엔 켜도 꺼도 아무 일도 안 났다). `--dart-define=DEMO=false`로도 끈다
      ⚠ 코스에서 500m 넘게 벗어나면 진행률을 갱신하지 않는다 — 없는 진행을 말하지 않으려고
      ⚠ 권한을 **먼저 묻고** 스트림을 연다. 안 물어보면 iOS에서 조용히 아무것도 안 온다
      거절하면 이유 + [설정 열기] [데모 모드로 보기]. 막지 않는다
      ⚠ simctl로는 "허용 안 함"을 누를 수 없어(`privacy revoke`는 '다시 묻기'로 돌린다)
        `test/dr00_test.dart` 위젯 테스트로 확인했다
- [x] **DR-06 백그라운드 근접 알림** (2026-08-29) — `core/proximity_alert.dart`
      발신 조건 = 시의성(장날·일몰·임박) **하나뿐**. 일반 스팟은 절대 안 나간다
      30분 1회 / 하루 4회, **횟수를 기기에 남긴다** (메모리면 껐다 켜서 우회된다)
      40분 무이동 → 무음 "레이더를 접어뒀어요" + 추적 중단
      DR-06a 유도 = **여행 2회 마친 사람이 🔔을 눌렀을 때만**. 온보딩에선 안 묻는다
      MY-03 「앱을 꺼둬도 알림」 토글 — 끄면 알림만 멈추고 OS 권한은 안 건드린다
      ⚠ `markSent`가 날짜를 안 찍어 횟수와 날짜가 따로 놀았다 (`test/proximity_test.dart`가 잡음)
      **iOS 기준으로 확정** (2026-08-29). SCREENS의 "Android 우선"을 뒤집었다
      iOS는 '항상 허용'을 안 받는다 — `UIBackgroundModes:location` +
      `allowBackgroundLocationUpdates`면 '앱을 사용하는 동안'만으로 백그라운드 위치가 온다
      **채널은 음성 + 알림 둘 다** (2026-08-29 결정). `UIBackgroundModes: audio` 추가 —
      이게 없으면 앱을 내린 순간 iOS가 낭독을 막는다. 앱을 살려두는 건 location이고
      audio는 그동안 말할 수 있게 해줄 뿐이다
      ⚠ **모의 주행에선 안 나간다.** 앱을 내리면 iOS가 재워서 타이머가 멈춘다.
        실제 주행에서만 확인되는 기능이라 MY-03에 이유를 한 줄로 적어뒀다
      ⚠ 실기기 확인 남음: 파란 표시줄 · 서스펜드 후 스트림 유지 · 카카오내비와 동시 실행
- [x] **고른 코스를 달린다** (2026-08-29) — `core/journey.dart`
      ⚠ 레이더가 어떤 코스를 골랐든 **데모 코스**를 돌리고 여행 기록에 `routeId: 7`을 박고 있었다.
        상단 뱃지와 "7번 국도 Nkm 기록 중"도 7 고정 — 전국 커버를 가정한 코드가 아니었다
- [x] **유령 여행기 제거** (2026-08-29) — 강제 종료된 주행이 0km·0곳 EP로 남았다
      끝맺음이 없어도 **뭐라도 남긴** 여행은 살린다. 달린 기록을 우리가 지울 수는 없다
- [x] **맵매칭** (2026-08-29) — `match_route_km(jsonb)` RPC, 여행을 마칠 때 1회
      지나온 점을 노선에 50m로 붙여 **노선별 km**를 낸다. 51선 수집이 이걸 쓴다
      ⚠ `geom::geography` 캐스팅이 **GiST 인덱스를 무력화**했다. 점 20개가 statement timeout —
        `&&` + `ST_Expand`로 도 좌표에서 먼저 거른 뒤 geography로 정확히 잰다 (0.2초)
      ⚠ PostGIS가 extensions 스키마라 **`&&`·`<->` 연산자가 기본 경로에 없다.**
        함수는 `extensions.ST_*`로 쓸 수 있어도 연산자는 안 된다 → `set search_path`
      ⚠ 검증이 잡은 것: 시작점만 보고 붙이니 **국도를 벗어난 구간까지 적립**됐다
        (65km 코스에 바다 좌표를 붙였더니 78km). **양 끝이 같은 노선일 때만** 센다.
        터널·앱 종료로 로그가 끊긴 구간을 위해 5km 상한도 뒀다
      ⚠ 예전 기록은 `routeKm`이 비어 있다 — 0으로 세면 이미 달린 길이 사라진다. 예전 방식으로 센다
      실측: 모의 주행 로그 77점 → 7번 국도 60.9km (기록 65km). 표본 간격 탓에 **적게** 잡힌다 —
        과다 적립보다 안전한 방향이다

## M4. 여행기 + 마이 (3일)

- [x] MY-02 여행기: 사진 콜라주 + 통계 칩 + 타임라인(visited·passed·skunked). 허탕은 흐리게
- [x] **그날 밤 하늘** (2026-08-29) — `night_sky_on(lat,lng,date)` RPC + `NightSky.line`
      `sun_moon_today`/`astro_today`는 **오늘**만 본다. 여행기는 지난 날을 보는 화면이라 따로 팠다
      ⚠ **달이 떠 있던 밤은 아무 말도 하지 않는다.** 데이터의 moonset은 그날 아침에 진 달이라
        '몇 시에 졌다'를 쓸 수 없다 (20260829190000 참고) — 틀린 문장보다 없는 줄이 낫다
      ⚠ 조사를 받침으로 가른다. '이(가)'로 얼버무리지 않는다 — 앉아서 읽는 화면이다
      실DB 확인: 8/29~8/31은 보름 직후라 줄이 안 뜬다(정상). **9/1~9/20이 달 없는 밤**
      ⚠ `sun_moon` 보유 범위가 **2026-08-29 ~ 10-12**다. 시연이 이 밖이면 파이프라인을 다시 돌려야 한다
- [x] **"계획에 없던 밥" 스탯** (2026-08-29) — `Trip.unplannedMeals(plannedSpotIds)`
      **이 앱이 하려는 일이 이 숫자 하나에 들어 있다** — 정해둔 대로가 아니라 지나다 걸린 곳
      `Trip`에 `courseId`를 남긴다. '계획'이 없으면 '계획에 없던'도 없다 → 코스 없는 여행은 0
      허탕이 있으면 그 자리를 **대신한다** (SCREENS 대로). 같은 크기·같은 모양, 담담한 톤
      ⚠ 화면에서 잡은 것: `tripFooter`가 `+\${km}km`로 이스케이프돼 **"+${km}km"가 그대로 찍혔다**
      ⚠ `radarRecording`도 같은 사고였고 아예 미사용이었다 — 레이더가 문구를 위젯에 직접 쓰고 있었다
        (CLAUDE.md "고정 카피는 strings.dart 단일 소스" 위반). 상수로 되돌렸다
- [x] **경로 저장** (2026-08-29) — `TripPoint(lat, lng, at)`, 여행 JSON에 함께 담는다
      사진 매칭·맵매칭 둘 다의 재료다. 그전엔 메모리에만 있어 앱을 끄면 사라졌다
      ⚠ **거리만으로 찍으면 멈춰 있는 동안이 경로에서 통째로 사라진다.** 사진은 대개
        멈춰서 찍는다 — 0.5km **또는** 2분(실시간) 간격으로 바꿨다
      ⚠ 점마다 전체를 다시 쓰지 않는다 (10점마다). 상한 5000점은 폭주 방어용
      ⚠ 복원 때 `_fromJson`을 두 번 불러 버릴 여행의 경로까지 싣고 있었다 — 한 번씩만 푼다
- [x] **사진 매칭** (2026-08-29) — `core/trip_photos.dart`
      여행 시간창(경로 첫 점 −10분 ~ 끝 점 +10분)의 사진만 조회. **밖은 아예 안 본다** —
      권한 문구 "여행한 시간대의 사진만 봅니다"가 사실이어야 한다
      EXIF GPS 있으면 그걸로, 없으면 촬영 시각을 경로에 보간. 300m 안의 들른 곳 이름을 붙인다
      ⚠ 0,0은 '없음'이지 좌표가 아니다. `TripStop`에 `lat/lng` 추가 (nullable — 0으로 안 채운다)
      ⚠ **기존 콜라주는 스팟 사진을 내 사진인 척** 보여주고 있었다. `+${photoCount-2}`라
        화면에 **"+-2"**가 찍혔다 (photoCount가 늘 0). 실제 사진 스트립으로 갈아치웠다
      ⚠ 사진이 없으면 스트립·캡션을 아예 안 그린다. 채우지 않는다
      ⚠ **실기기 확인 남음** — 시뮬레이터는 `simctl privacy grant photos`가 안 먹고
        자동 탭도 막혀(osascript 보조 접근 없음) 권한 팝업을 넘길 수가 없다
- [x] MY-02 화면 (2026-08-29) — EP 넘버링 · 타임라인 · 사진 스트립 · 그날 밤 하늘 · '하루 더?'
      ⚠ 푸터가 `+${km}km`로 이스케이프돼 **화면에 그대로 찍히고 있었다** — 스크린샷이 잡았다
- [x] **공유 카드** (2026-08-29) — `features/my/share_card.dart` (딥링크는 남음)
      **보여주고 나서 보낸다.** 무엇이 나가는지 본 뒤 사용자가 누른다 — 경로가 담긴 이미지다
      ⚠ **카카오맵을 캡처할 수 없다.** 플랫폼 뷰라 `RepaintBoundary`에 안 잡혀 빈 자리가 나온다.
        실제 GPS 점으로 직접 그린다 — 정확하기도 하다
      ⚠ 시작·끝 300m는 **아예 잘라낸다** (`core/geo.dart trimEnds`). 흐리게 하지 않는다 —
        가린 척하는 건 안 가린 것이다. 자르고 두 점이 안 남으면 지도를 그리지 않는다
      ⚠ 상자를 경로 모양에 맞춘다. 남북으로 긴 국도를 넓은 상자에 넣으면 좌우가 텅 빈다
      ⚠ 시트는 탭이 있어야 열려 시뮬레이터로 못 본다 → **골든 테스트로 실물을 뽑아 확인**했다
        (`test/goldens/share_card.png`, Pretendard를 FontLoader로 실어야 글자가 나온다)
      ⚠ 골든이 잡은 것: 칩에 `alignment`를 줘서 폭이 최대로 팽창해 세로로 쌓였다.
        `_statChips`에 같은 주석이 있는데 그대로 밟았다
- [x] MY-01/03: 51선 진행률(여행기에서 산출), 찜/스쳐간 세그먼트, 여행기 리스트, 설정
- [x] ~~카카오 로그인 (Supabase Auth)~~ → **취소 (2026-08-29). 로그인을 넣지 않는다** (원칙 5)

## M5. 폴리시 & 데모 준비 (1.5일)

- [x] TTS: 근접 카드 낭독, 무응답 15초 → **스쳐간 발견** (M3에서 완료. '찜'이 아니다)
- [x] **원칙 전수 검수 1차** (2026-08-29) — 고정 카피 152개 + 위젯 직접 문구 훑음
      재촉(카운트다운·"빨리"·"놓치지 마세요") **0건**, 별점·랭킹·점수 **0건**,
      예약·결제 플로우 **0건** (전부 "예약은 외부에서 — 여기선 위치만" 문맥)
      ⚠ **화면에 나가던 지어낸 값 3건을 걷어냈다** — 아래 참조
- [x] **CO-06b 실데이터 연결** (2026-08-29) — `compare_routes` Edge Function 배포
      카카오모빌리티 길찾기 REST 2회(기본 / `avoid=motorway`)로 고속도로↔국도를 비교한다
      **실측: 삼척→강릉 고속도로 44분 vs 국도 62분**, 서울→강릉 148분 vs 230분
      ⚠ 키는 `supabase secrets`에만 있다. 앱은 **분 두 개만** 받는다 —
        경로 좌표를 안 받는 게 구조적 방어다 (받으면 그려보고 싶어진다)
      ⚠ Edge Function이 스스로 거절하는 경우: 키 없음 / 경로 없음 / **국도가 더 빠름**.
        마지막은 '느린 길을 권하는' 이 모달의 전제가 깨지기 때문이다
      ⚠ 앵커는 **오늘 장날만** 본다. SCREENS는 행사·일몰도 허용하지만 그 문구 모양이
        승인된 적 없어 임의로 만들지 않았다 — 필요하면 카피부터 정할 것
- [x] ~~CO-06b 지어낸 소요시간 제거~~ (2026-08-29, 위에서 실데이터로 대체)
      `'2시간 10분' / '2시간 50분'`이 하드코딩돼 화면에 나가고 있었다.
      §3.7은 길찾기 REST 1회 조회가 근거인데 **키가 아직 없다**. 근거 없이 40분을 더 쓰라고
      설득하는 건 이 예외를 승인한 이유와 정반대다 → `hasRealComparison = false`로 **모달을 끈다**.
      화면(build)은 완성돼 있어 키가 생기면 상수만 true로 바꾸면 된다
- [x] **가짜 최근 검색 제거** (2026-08-29) — 한 번도 검색 안 한 사람에게
      '묵호등대·5일장·해수욕장'을 보여주고 있었다. SharedPreferences에 진짜로 쌓는다.
      ⚠ 테스트가 **그 가짜가 보이는지 검사**하고 있었다 — 없는 걸 있다고 검증하던 셈
- [x] **없는 코스로 가던 경로 수정** (2026-08-29) — 거점 역진입이 `/course/donghae-sea`로
      갔는데 **그런 코스는 없다**(실제 id는 uuid). 에러 화면이 떴다. `kDemoCourseId` 하나로 모음
- [x] **거점 좌표 0,0 방어** (2026-08-29) — `_pickedLat ?? 0`이 남아 있었다.
      좌표를 모르면 거점으로 삼지 않는다 — 0,0은 기니만이고 내비가 대서양으로 안내한다
- [ ] 알림 톤·문구 최종 검수 2차 — 실기기 화면 캡처로 전수
- [ ] 시연 리허설: 홈→국도→코스→거점 핀→출발→[시뮬 주행]→장날 카드→들르기→
      스쳐감→여행 종료→여행기→공유→마이 진행률
- [x] ~~빌드: Android APK~~ → **취소 (2026-08-29). iOS 전용으로 확정** — 시연·제출 모두 iOS

---

## 리스크 조기 검증 (M0.5~M1 중, 순서 무관)

- [ ] 카카오내비 viaList **실기기** 테스트 (에뮬레이터 불가) — 코드 완료, 눈으로 확인만
- [x] 연관관광지 **2시점 확보됨** (202603/202606). 데이터랩은 대체돼 불필요
- [ ] photo_manager iOS 권한(limited access) **실기기** 확인 — 시뮬레이터는 팝업을 넘길 수 없다
- [x] ~~DR-06 Android 포그라운드 서비스 확인~~ → **취소 (2026-08-29). iOS 전용**
- [ ] 7번 국도 데모 구간 실주행 1회 (가능하면) — 발견 타이밍 체감 조정

## 데모 컷라인 (시간 부족 시 버리는 순서)

1. TTS → 2. 온보딩 → 3. DR-06 백그라운드 알림 → 4. 몰아보기 카드 →
5. DR-05 동승자 모드 → 6. 사진 시각 보간(EXIF GPS만) → 7. CO-06b 국도 제안

**절대 못 버림**: 레이더 + 장날 카드, 내비 핸드오프, 여행기 생성, 거점 핀

> ⚠ 3·5·7번은 이번에 MVP로 승격된 항목이다. 마감일이 확정되면 컷라인을 다시 조정할 것.
