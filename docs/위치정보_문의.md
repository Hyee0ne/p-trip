# 위치정보 Q&A 게시글 (그대로 복사해서 올리면 됩니다)

> 문의처 — KISA 위치정보 상담 ☎ **118** / 위치정보지원센터 게시판
> ⚠ 마감(2026-09-21)이 임박하면 **전화가 가장 빠릅니다.**

---

## 제목

```
식별자 없이 위경도만 서버로 보내 조회에만 쓰는 경우 위치기반서비스사업자 신고 대상인가요
```

---

## 본문 (복사용)

```
안녕하세요. 개인 개발자입니다.
App Store에 출시한 여행 앱의 신고 대상 여부를 확인하고 싶습니다.

■ 어떤 앱인가요
국도를 달리는 동안 현재 위치 반경 5km 안의 관광지·음식점을 알려주는 앱입니다.
한국관광공사 공공데이터를 씁니다. 회원가입과 로그인이 없습니다.

■ 위치를 이렇게 다룹니다
1) 앱이 서버에 "이 좌표 주변에 뭐가 있나요" 하고 위경도를 보냅니다.
2) 서버는 그 좌표로 데이터베이스를 조회해 주변 장소 목록만 돌려줍니다.
3) 좌표는 저장하지 않습니다. 조회 조건으로만 쓰고 버립니다.

■ 함께 보내지 않는 것
- 회원 ID, 계정 (로그인 기능 자체가 없습니다)
- 기기 식별자, 광고 식별자
- 주행 경로, 방문 기록, 저장한 장소 (전부 휴대폰 안에만 있습니다)

즉 서버에는 "누구"는 없고 "어느 지점"만 전달됩니다.
좌표를 받는 서버 함수 5개는 모두 데이터베이스를 변경할 수 없도록
읽기 전용(STABLE)으로 선언되어 있어, 구조적으로 저장이 불가능합니다.

■ 질문드립니다

Q1. 로그인이 없고 식별자도 함께 보내지 않는 상태에서, 위경도만 보내
    조회 조건으로 쓰고 저장하지 않는 경우에도 '개인위치정보'로 보아
    위치기반서비스사업자 신고 대상이 되는지요?

Q2. 안내에 "서버 DB 저장 여부와 무관하게 전송 자체만으로 대상"이라고 되어 있는데,
    여기서 말하는 전송이 특정 개인과 연결될 수 있는 경우를 전제하는 것인지,
    아니면 식별자가 없는 좌표도 포함되는 것인지 궁금합니다.

Q3. 저희가 저장하지 않더라도 클라우드 서비스(Supabase) 의 요청 기록에
    좌표가 남을 수 있습니다. 그 기록을 조회하거나 활용하지 않아도
    '전송·활용'에 해당하나요?

Q4. 신고 대상이 아니려면 어느 정도까지 바꿔야 하는지요? 두 가지를 검토 중입니다.
    (가) 좌표를 약 25km 격자로 뭉개서 보내고, 정확한 거리 계산은 휴대폰에서 수행
    (나) 지역 단위 데이터를 미리 내려받아, 좌표를 아예 서버로 보내지 않음

Q5. 이미 출시된 상태입니다. 신고 대상이라면 신고가 끝날 때까지 서비스를
    중단해야 하는지, 아니면 신고를 진행하면서 운영할 수 있는지요?

■ 참고로 이미 갖춘 것
- 개인정보처리방침 게시
- 위치 권한은 '앱을 사용하는 동안'만 요청 ('항상 허용' 안 받음)
- 백그라운드 위치는 기본 꺼짐, 사용자가 켠 경우에만 동작하며
  동작 중에는 iOS 파란 표시줄이 계속 노출됩니다

바쁘신 중에 감사합니다.
```

---

## 답변에 따라 할 일

| 답변 | 할 일 |
|---|---|
| 신고 대상 아님 | 그대로 운영. 코드 변경 없음 |
| 격자화면 비신고 (Q4-가) | 반나절 작업 — `coverageKey` 방식을 레이더에도 적용 |
| 전송 자체가 문제 (Q4-나) | 1~2일 — 지역 단위 선다운로드. 오프라인 동작이 덤으로 따라옴 |
| 신고해야 함 | 신고 절차 진행. Q5 답에 따라 운영 지속 여부 결정 |

---

# 답변 도착 (2026-09-07) — 요지와 우리 상태

## 담당자 답변 요지

| 질문 | 답 |
|---|---|
| Q1 식별자 없는 좌표도 개인위치정보인가 | **아닐 가능성이 있다.** 식별자를 함께 보내지 않고, 사업자가 가진 다른 정보와 결합해도 개인을 특정할 수 없는 구조라면 신고 대상이 아닐 수 있다 |
| Q2 '전송 자체만으로 대상' 의 뜻 | 저장 안 해도 대상이 될 수 있다는 뜻이지, **전송된 모든 좌표가 개인위치정보라는 뜻은 아니다.** 특정 개인과 연결되는지를 함께 본다 |
| Q3 클라우드 요청 기록 | **여기가 쟁점이다.** 앱 DB에 저장하지 않는 것과 클라우드 로그에 남는 것은 구분해서 봐야 한다. `STABLE` 선언은 DB 저장이 없다는 근거는 되지만 **인프라 로그까지 의미하지 않는다.** 실제 로그 생성·보관 구조를 확인할 것 |
| Q4 어느 구조가 명확한가 | **(나) 단말 내 처리가 더 명확하다.** (가) 격자화는 "정확한 좌표가 아니다"만으로 단정하기 어렵다 |
| Q5 출시 상태 운영 | 대상이면 신고 진행. 운영 지속 여부는 **법률 전문가에게 별도 확인** |

⚠ 개인정보처리방침·iOS 위치 권한·백그라운드 위치는 **위치정보법 신고와 별개 검토사항**이라고 명시했다.

## 코드 실측 — 좌표가 서버로 나가는 모든 경로

| 흐름 | 좌표 정밀도 | 어디로 | 실리는 곳 |
|---|---|---|---|
| `discover_ahead` (레이더 큐) | **0.01° ≈ 1.1km 격자** | 우리 DB | POST 본문 |
| `discover_ahead` (지역 준비 판정) | **0.25° ≈ 28km 격자** | 우리 DB | POST 본문 |
| `sun_moon_today` (일몰) | **0.1° ≈ 11km 격자** | 우리 DB | POST 본문 |
| `nearby_routes` (내 주변 국도) | ⚠ **원본** | 우리 DB | POST 본문 |
| `route_path_ahead` (앞길 선형) | ⚠ **원본** | 우리 DB | POST 본문 |
| `compare_routes` (거점 역진입) | ⚠ **원본(거점)** | Edge Fn → 카카오모빌리티 | POST 본문 → 외부 |
| `search_places` (거점 검색) | ⚠ **원본** | Edge Fn → 카카오 | POST 본문 → **외부 URL 쿼리** |

**유리한 사실 (코드로 확인함)**
- 좌표는 **언제나 POST 본문**이다. 우리 쪽 URL에 실리는 곳이 하나도 없다 →
  게이트웨이 액세스 로그(메서드·경로·상태·IP)에 좌표가 안 남는다
- Edge Function 두 개에 **`console.log` 가 하나도 없다** → 함수 로그에 좌표가 안 남는다
- 가장 잦은 흐름(레이더)은 이미 **1.1km 격자**다. 원래는 provider 캐시 때문에 넣은 건데
  결과적으로 정확한 좌표가 안 나간다
- 좌표를 받는 DB 함수는 전부 `STABLE` — 구조적으로 저장이 불가능하다

**남은 구멍**
- `nearby_routes` · `route_path_ahead` 가 **원본 좌표**를 보낸다
- `search_places` 가 좌표를 **카카오 URL 쿼리(`x`,`y`)** 로 넘긴다 — 외부 액세스 로그에 남는 형태

## 해야 할 일

### ① 클라우드 로그 확인 (담당자가 지목한 Q3) — **확인 완료 (2026-09-07)**

**ⓐ 스키마** — Supabase 공식 문서. `edge_logs` 는 요청 헤더를 **허용목록**으로만 기록한다:
`accept · cf-connecting-ip · cf-ipcountry · host · user-agent · x-forwarded-proto ·
referer · content-length · x-real-ip · x-client-info · x-forwarded-user-agent · range · prefer`
→ **요청 본문 필드가 아예 없다.** 우리 좌표는 전부 POST 본문이다.
→ 단 **IP 는 남는다** (`cf-connecting-ip`, `x-real-ip`).

**ⓑ 실제 로그 화면** — 대시보드에서 눈으로 확인했다.
```
POST | 200 | .../rest/v1/rpc/sun_moon_today                                   ← 경로만
GET  | 200 | .../rest/v1/sun_moon?select=locdate&order=locdate.desc&limit=1   ← 쿼리스트링 통째로
```
GET 은 쿼리스트링이 그대로 찍히는데 **POST RPC 는 경로만 찍힌다.** 스키마대로다.
같은 시각 `Error 5xx = 0` · `Warning 4xx = 0`, Postgres 로그는 `checkpoint` 메시지뿐이었다
— 쿼리문도 파라미터도 없다.

**ⓒ 보관 기간** — 공식 가격표. 우리는 **Free 플랜 = 1일**.
(Pro 7일 · Team 28일 · Enterprise 90일)
→ 설령 어딘가에 좌표가 실려도 **24시간 뒤 사라진다.** '보유하지 않는다'의 근거다.

⚠ 유일하게 남는 이론적 구멍: 쿼리가 **실패하면** `postgres_logs.event_message` 에
  문장과 파라미터가 실릴 수 있다. 다만 거기엔 앱 사용자 IP 가 없고(풀러 연결),
  Free 플랜이라 하루면 사라지며, 확인 시점에 5xx 가 0이었다.
  → ②(격자화)를 하면 이 경우에도 찍히는 건 **한 사람의 위치가 아니라 격자 한 칸**이 된다.

### ② 값싼 보강 (약 30분)
남은 원본 좌표 3곳을 전부 격자화한다. 그러면 **"정확한 좌표는 기기를 떠나지 않는다"** 가
문장이 아니라 사실이 된다. 담당자가 (가)만으로는 단정 못 한다고 했지만,
'식별자 없음 + 저장 없음 + 로그 없음' 과 합쳐질 때의 무게는 다르다.

⚠ **①에서 확인된 유일한 구멍(오류 로그)을 구조적으로 막는 방법이기도 하다.**
  보내는 좌표가 애초에 1.1km 격자면, 오류 로그에 그대로 찍혀도 **한 사람의 위치가 아니라
  격자 한 칸**이다. 로그 설정을 우리가 못 바꾸니 보내는 값을 바꾸는 게 우리가 쥔 손잡이다.

### ③ 근본 구조 (1~2일) — 담당자가 권한 (나)
지역 단위 데이터를 미리 내려받아 좌표를 아예 안 보낸다.
⚠ **전국 스팟이 3,341건(약 1MB)이라 통째로 내려받을 수 있다.** 지역을 나눌 필요도 없다.
`sun_moon` 도 41칸 × 30일 = 1,230행이라 같이 내려받으면 된다.
그러면 레이더·일몰·국도 판정이 전부 기기 안에서 끝나고, 남는 건 사용자가 직접 누르는
거점 검색(카카오)뿐이다. **오프라인 주행이 덤으로 따라온다** — 국도엔 음영지역이 많다.

### ④ 마감(2026-09-21) 기준 판단
답변이 "신고 대상이 아닐 수 있다" 쪽이므로 **출시를 멈출 이유는 없다.**
①을 확인하고 ②를 적용한 뒤, ③은 공모전 이후로 미루는 것이 현실적이다.
⚠ 다만 Q5는 법률 전문가 확인 사항으로 남겨두었다 — ①에서 좌표가 로그에 남는 것이
  확인되면 그때는 ③을 앞당기는 게 맞다.
